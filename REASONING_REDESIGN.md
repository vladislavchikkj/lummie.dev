# Reasoning Display Redesign

## Обзор изменений

Был выполнен редизайн компонентов отображения процесса генерации проекта, чтобы сделать их более информативными и визуально привлекательными, используя компоненты из `@/components/ui/shadcn-io/ai`.

## Измененные файлы

### 1. `reasoning-display.tsx`
**Основные изменения:**
- Интеграция компонентов `Reasoning`, `ReasoningTrigger`, `ReasoningContent` из AI UI kit
- Добавлена логика группировки thinking событий (объединение started/completed пар)
- Улучшена система иконок для разных типов действий:
  - 🔍 `Search` - для "Check" и "Read" действий
  - 📄 `FileText` - для "Create" и "Save" действий
  - 🔧 `Wrench` - для "Review", "Build", "Work" действий
  - ✓ `CheckCircle2` - для остальных завершенных действий
  - ⏳ `Loader` - для действий в процессе выполнения

**Новые возможности:**
- Thinking события теперь отображаются как раскрывающиеся блоки "Thought for Xs"
- Action события показываются как простые строки с иконками
- Поддержка streaming режима для отображения текущего процесса

### 2. `reasoning-loading.tsx`
**Изменения:**
- Обновлен UI для пустого состояния (используется `BrainIcon` вместо `Circle`)
- Добавлен проп `isStreaming={true}` при передаче в `ReasoningDisplay`

### 3. `functions.ts` (Inngest)
**Структура событий:**

```typescript
// Thinking events (пары started/completed)
{
  type: 'thinking',
  phase: 'started',
  title: 'Thinking',
  description: 'Описание процесса размышления',
  timestamp: number
}
{
  type: 'thinking',
  phase: 'completed',
  title: 'Thinking',
  timestamp: number,
  duration: number
}

// Action events
{
  type: 'action',
  phase: 'started' | 'completed',
  title: 'Название действия',
  description?: 'Опциональное описание',
  timestamp: number,
  duration?: number
}
```

**Последовательность событий:**
1. Thinking (started/completed) - "Initializing workspace and checking project status"
2. Action (completed) - "Checked project structure"
3. Action (started/completed) - "Creating/Created sandbox environment"
4. Thinking (started/completed) - "Analyzing requirements and designing solution architecture"
5. Action (completed) - "Read layout"
6. Thinking (started/completed) - "Building your project and implementing requirements"
7. Action (completed) - "Created project structure"
8. Action (started/completed) - "Reviewing work"

## Визуальное отображение

### Завершенные события
```
🧠 Thought for 2s ▼ (раскрывающийся блок)
   └─ Описание процесса размышления

🔍 Checked project structure

🧠 Thought for 3s ▼
   └─ Analyzing requirements...

📄 Created project structure
   └─ Generated all necessary files and components
```

### Текущий процесс (streaming)
```
🧠 Thinking... (открыт по умолчанию)
   └─ Building your project...

⏳ Reviewing work (анимированный loader)
   └─ Verifying implementation...
```

## Использование

### В компоненте сообщений
```tsx
// Для завершенных сообщений
<ReasoningDisplay events={message.reasoningSteps} />

// Для realtime процесса
<ReasoningDisplay events={visibleEvents} isStreaming={true} />
```

## Преимущества нового дизайна

1. **Улучшенная читаемость** - thinking события скрыты по умолчанию после завершения
2. **Визуальная иерархия** - четкое разделение между размышлениями и действиями
3. **Информативность** - подробные описания для важных этапов
4. **Интерактивность** - возможность раскрыть/скрыть детали размышлений
5. **Анимации** - плавные переходы и индикаторы прогресса
6. **Семантические иконки** - быстрая идентификация типа действия

## Технические детали

### Группировка событий
Компонент автоматически объединяет пары событий (started + completed) в одно отображаемое событие:

**Для thinking событий:**
- Description из started события (для контента)
- Duration из completed события (для отображения времени)

**Для action/step событий:**
- Полностью пропускается started событие
- Показывается только completed событие с duration

**Алгоритм группировки:**
- Проходит по всему массиву событий
- Для каждого started события ищет matching completed событие во всем оставшемся массиве (не только в следующем элементе)
- Matching определяется:
  - Для thinking: по типу события
  - Для action/step: по типу И title события
- Это позволяет корректно группировать события даже если между started и completed есть другие события

### Управление состоянием
Компонент `Reasoning` автоматически:
- Открывается при начале streaming
- Закрывается с задержкой 1с после завершения
- Отслеживает и отображает duration

### Отступы
ReasoningDisplay использует те же отступы что и MessageContent для assistant сообщений:

**Внешние отступы (margin):**
- `ml-0` на мобильных устройствах
- `sm:ml-4` на desktop (16px)

**Внутренние отступы (padding):**
- `px-3` на мобильных (12px горизонтальный padding)
- `sm:px-4` на desktop (16px горизонтальный padding)

**Итого визуальное выравнивание слева:**
- Мобильные: `0px (margin) + 12px (padding) = 12px`
- Desktop: `16px (margin) + 16px (padding) = 32px`

Это полностью совпадает с отступами MessageContent и обеспечивает идеальное визуальное выравнивание reasoning блока с контентом сообщений бота.

## Исправленные проблемы

### 1. Бесконечная загрузка "Reviewing work"
**Проблема:** Событие "Reviewing work" показывало loader бесконечно, даже после завершения генерации.

**Решение:** 
- Финальное событие завершения теперь добавляется в массив `reasoningSteps` ДО сохранения в БД
- Событие также публикуется в realtime канал ПОСЛЕ сохранения файлов
- Это гарантирует, что событие завершения присутствует как в сохраненных данных, так и в realtime обновлениях

### 2. Несовпадение отступов
**Проблема:** Левый отступ у блока reasoning не совпадал с отступом сообщений бота.

**Причина:** У MessageContent есть как внешний margin (`ml-0 sm:ml-4`), так и внутренний padding (`px-3 sm:px-4`), а у reasoning блока изначально был только margin.

**Решение:**
- Добавлены классы `ml-0 sm:ml-4` (внешний margin) к обертке `ReasoningDisplay`
- Добавлены классы `px-3 sm:px-4` (внутренний padding) к обертке `ReasoningDisplay`
- Те же классы добавлены к обертке `ReasoningRealtime`
- Теперь визуальное выравнивание полностью совпадает с `MessageContent`:
  - Мобильные: `0px + 12px = 12px` слева
  - Desktop: `16px + 16px = 32px` слева

### 3. Description отображается после завершения
**Проблема:** Описание action события (например, "Verifying implementation and preparing deployment") оставалось видимым даже после завершения действия.

**Решение:**
- Description теперь показывается только для событий в процессе выполнения (`loading === true`)
- После завершения события показывается только title и duration
- Это делает интерфейс более чистым и понятным

**Поведение:**
```
Во время выполнения:
⏳ Reviewing work
   └─ Verifying implementation and preparing deployment

После завершения:
✓ Reviewing work  2s
```

### 4. Action события не группируются (крутящийся loader после завершения)
**Проблема:** После завершения генерации событие "Reviewing work" продолжало показывать крутящийся loader и description, создавая впечатление что процесс еще идет.

**Причина:** Группировка работала только для thinking событий, но не для action/step событий. Когда приходили события "Reviewing work (started)" и "Reviewing work (completed)", они оба показывались отдельно.

**Решение:**
- Добавлена группировка для action и step событий по тому же принципу что и для thinking
- Алгоритм теперь ищет matching completed событие по всему массиву, а не только в следующем элементе
- Это позволяет корректно обрабатывать ситуации когда между started и completed есть другие события
- Для action/step событий показывается только completed версия, started полностью пропускается

**Результат:**
```
До исправления:
⏳ Reviewing work (показывается даже после завершения)
   └─ Verifying implementation and preparing deployment

После исправления:
✓ Reviewing work  2s (только completed событие)
```

