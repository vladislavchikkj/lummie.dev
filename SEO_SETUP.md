# SEO Setup Guide

Этот файл содержит инструкции по настройке SEO для lummie.app после внесенных изменений.

## ✅ Выполненные изменения

1. ✅ Google Search Console verification настроен через переменную окружения
2. ✅ Подготовлен компонент для Google Analytics
3. ✅ Добавлен статический SEO контент на главную страницу
4. ✅ Проверен sitemap.ts - все страницы существуют
5. ✅ Улучшена JSON-LD схема

## 🔧 Шаги для полной настройки

### 1. Google Search Console

1. Зайдите в [Google Search Console](https://search.google.com/search-console)
2. Добавьте свой домен `lummie.app`
3. Выберите метод верификации "HTML tag"
4. Скопируйте значение из атрибута `content` (это будет код вида `AbC_123_...`)
5. Добавьте в ваш `.env.local` файл:
   ```env
   NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=AbC_123_Ваш_Реальный_Код
   ```
6. Задеплойте изменения
7. Вернитесь в Google Search Console и нажмите "Verify"
8. После верификации, в разделе "Sitemaps" добавьте: `https://lummie.app/sitemap.xml`

### 2. Google Analytics 4 (GA4)

1. Создайте аккаунт в [Google Analytics](https://analytics.google.com/)
2. Создайте новое свойство (Property) для вашего сайта
3. Получите Measurement ID (формат: `G-XXXXXXXXXX`)
4. Установите пакет:
   ```bash
   npm install @next/third-parties
   ```
5. Раскомментируйте код в файле `src/components/google-analytics.tsx`
6. Добавьте в ваш `.env.local` файл:
   ```env
   NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
   ```
7. Задеплойте изменения

### 3. Проверка работы

После деплоя проверьте:

1. **Google Search Console:**
   - Убедитесь, что сайт верифицирован
   - Проверьте, что sitemap отправлен и проиндексирован
   - Проверьте покрытие индексации

2. **Google Analytics:**
   - Убедитесь, что данные начинают поступать в Real-time отчеты
   - Проверьте, что события отслеживаются корректно

3. **SEO метатеги:**
   - Проверьте через [Google Rich Results Test](https://search.google.com/test/rich-results)
   - Убедитесь, что JSON-LD схемы валидны

## 📝 Переменные окружения

Добавьте следующие переменные в ваш `.env.local` (для локальной разработки) и в настройки вашего хостинга (для production):

```env
# Google Search Console Verification
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=your_verification_code_here

# Google Analytics 4 Measurement ID
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
```

## 📊 Мониторинг

Регулярно проверяйте:

1. **Google Search Console:**
   - Покрытие индексации
   - Производительность в поиске
   - Проблемы с индексацией

2. **Google Analytics:**
   - Трафик и источники
   - Поведение пользователей
   - Конверсии (если настроены)

## 🔍 Дополнительные улучшения (опционально)

1. Добавьте реальные социальные сети в `organizationData.sameAs` в `src/app/(home)/page.tsx`
2. Обновляйте `dateModified` в JSON-LD схеме при крупных обновлениях
3. Добавьте больше структурированных данных (FAQPage, BreadcrumbList и т.д.) при необходимости
4. Регулярно обновляйте контент на главной странице для поддержания актуальности

## ⚠️ Важные замечания

- Не используйте `aggregateRating` в JSON-LD без реальных отзывов (это может быть расценено как спам)
- Убедитесь, что все страницы из sitemap реально доступны и возвращают 200 статус
- Проверяйте производительность сайта - медленные сайты ранжируются хуже
